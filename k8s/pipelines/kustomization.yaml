apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Adds hybrid-cloud-demo label to all resources.
commonLabels:
  app.kubernetes.io/part-of: hybrid-cloud-demo

resources:
  - cloud-identifer-task.yaml
  - skupper-site-task.yaml
  - kn-skupper-service-task.yaml
  - ksvc-deploy.yaml
  - svc-deploy.yaml
  - skupper-site-deploy.yaml
# bases:
#   - ../
